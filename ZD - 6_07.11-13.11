#include <иопоток>
использование пространства имен std;

основной ()
{
    инт н;
    логический к = ложь;
    cout << "n="; цин >> п;

    двойной* C = новый двойной[n];

    cout << "Введите " << n << " элементы:\n";
    для (int i = 0; i < n; i++)
    {
        cin >> C[i];
        если (C[i] == 0,0) k = верно;
    }

    если (к)
    {
        для (int i = 0; i < n; i++)
            if (C[i] == 0.0) cout << "Ответ: " << i << " ";
        cout << "\n";
    }
    иначе cout << "НЕТ\n";

    удалить[]С;
    система("пауза");
    вернуть 0;
}
