#include <иопоток>
использование пространства имен std;

основной ()
{
    инт н;
    cout << "введите n: ";
    цин >> п;
    если (n < 100 || n>999)
        cout << "ошибка n";
    еще
    {
        в а, б, в;
        а = н/100;
        б = (n/10) % 10;
        с = п % 10;

        целое разрешение;
        рез = а + б + с;
        cout << "ответ: " << res;
    }
    вернуть 0;
}
